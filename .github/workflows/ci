# name: unittest

# on:
#   push:
#     branches: ["main"]
#   pull_request:
#     branches: ["main"]

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
#       matrix:
#         python-version: ["3.8"]

#     steps:
#       - uses: actions/checkout@v3

#       - name: Set up Python ${{ matrix.python-version }}
#         uses: actions/setup-python@v3
#         with:
#           python-version: ${{ matrix.python-version }}

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           python -m pip install wheel
#           python -m pip --no-cache-dir install torch==1.12.0
#           if [ -f requirements_cpu.txt ]; then python -m pip install -r requirements_cpu.txt; fi

#       - name: Install pyggnn
#         run: |
#           pip install -e .[test]

#       - name: Unittest
#         run: |
#           python -m unittest discover -s ./src/tests -p "test_*.py"
